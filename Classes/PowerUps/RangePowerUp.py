from Classes.PowerUps.PowerUp import PowerUp

class RangePowerUp(PowerUp):
    def __init__(self,projectile):
        self.projectile = projectile


    def Apply(self): #Changes the timer within projectile so the projectile lasts longer
        pass
